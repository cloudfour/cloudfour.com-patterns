import { Meta } from '@storybook/addon-docs/blocks';
import tokens from '../compiled/tokens/js/tokens';
const _ = require('lodash');
const aspectRatios = tokens.number.aspect_ratio;
const aspectRatioRows = Object.keys(aspectRatios).map((key) => {
  const { name, attributes, value } = aspectRatios[key];
  return (
    <tr key={name}>
      <td>
        <code>aspect-ratio.${_.kebabCase(attributes.item)}</code>
      </td>
      <td>
        <code>{value}</code>
      </td>
    </tr>
  );
});

<Meta title="Design/Tokens/Aspect Ratio" />

# Aspect Ratio

```scss
@use "../../compiled/tokens/scss/aspect-ratio";
$example: aspect-ratio.$wide; // => '16/9'
```

```javascript
import tokens from '../../compiled/tokens/js/tokens';
console.log(tokens.number.aspect_ratio.wide.value); // => '16/9'
```

## Ratio

<table>
  <thead>
    <tr>
      <th style={{ width: '10%' }}>Name</th>
      <th>Value</th>
    </tr>
  </thead>
  <tbody>{aspectRatioRows}</tbody>
</table>
