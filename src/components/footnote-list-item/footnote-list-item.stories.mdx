import { Story, Canvas, Meta } from '@storybook/addon-docs/blocks';
import template from './footnote-list-item.twig';

<Meta
  title="Components/Footnote/Footnote List Item"
  argTypes={{
    count: { type: { name: 'number' } },
  }}
/>

# Footnote List Item

Used for displaying the content of a footnote.

## Basic usage

<Canvas>
  <Story name="Basic">{(args) => template(args)}</Story>
</Canvas>

## Template Properties

- `id` (number)

## Template Properties

- `count` (number)
