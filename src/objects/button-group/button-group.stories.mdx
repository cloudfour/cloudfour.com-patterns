import { Story, Canvas, Meta } from '@storybook/addon-docs/blocks';
// Import articles from './demo/articles.json';
import basicDemo from './demo/basic.twig';
import template from './button-group.twig';
const basicDemoStory = (args) => {
  return basicDemo({ ...args });
};

<Meta title="Objects/Button Group" />

# Button Group

The Button Group object pattern can be used for a collection of buttons.

## Default spacing

Add the `o-button-group` CSS class to the wrapper container.

<Canvas>
  <Story name="Default">{basicDemoStory.bind()}</Story>
</Canvas>

## Tight spacing

There is also a modifier, `o-button-group--tight`, to tighten up the spacing between the buttons.

<Canvas>
  <Story name="Tight" args={{ class: 'o-button-group--tight' }}>
    {basicDemoStory.bind()}
  </Story>
</Canvas>
