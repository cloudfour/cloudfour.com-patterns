{#
  We could use `{% block content %}` but Twig.js complains when nested in
  Storybook for some reason... 🤔
#}
{% include '../button.twig' with { label: 'Default' } %}
{% include '../button.twig' with { label: 'Secondary', class: 'o-button--secondary' } %}
{% include '../button.twig' with { label: 'Tertiary', class: 'o-button--tertiary' } %}
