import { Story, Canvas, Meta } from '@storybook/addon-docs/blocks';
import introDemo from './demo/demo.twig';

<Meta title="Objects/Featured Row" />

# Featured Row

The Featured Row layout object can be used by individual sections on a page to place content. It is helpful for highlighting a section by giving it a distinct layout. It includes a heading, main content, and optional link.

<Canvas>
  <Story name="Default">{introDemo}</Story>
</Canvas>

## Template Properties

- `heading` (block): Block to add the heading.
- `content` (block): Block to add the main content.
- `action` (block): Block to add a cta.
