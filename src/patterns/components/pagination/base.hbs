---
notes: |
  Show several page links in a row with optional visible controls for "next" and "previous" pages.

  If there are too many links to show in the given space, they are hidden. "Next" and "previous" controls are never hidden.
---
<nav role="menu" aria-label="Pagination">
  <ul class="Pagination Pagination--withNext Pagination--withPrevious">
    {{#iterate (defaultTo pages 10)}}
      <li>
        {{#compare @count "===" (defaultTo current 2)}}
          <span class="Pagination-item">
            <span class="u-hiddenVisually">Currently viewing page</span>
            {{@count}}
          </span>
        {{else}}
          <a class="Pagination-item" href="#">
            {{! TODO: Make pagination states dynamic and based on incrementing or decrementing current }}
            {{#compare @count "===" 1}}
              <span class="Pagination-item Pagination-item--previous">
                {{#embed "patterns.components.icon.base"
                  iconid="arrow-left"
                  labelledby="pagination-previous-title"}}
                  {{#content "before"}}
                    <title id="pagination-previous-title">Previous:</title>
                  {{/content}}
                {{/embed}}
              </span>
            {{/compare}}
            {{#compare @count "===" 3}}
              <span class="Pagination-item Pagination-item--next">
                {{#embed "patterns.components.icon.base"
                  iconid="arrow-right"
                  labelledby="pagination-next-title"}}
                  {{#content "before"}}
                    <title id="pagination-next-title">Next:</title>
                  {{/content}}
                {{/embed}}
              </span>
            {{/compare}}
            <span class="u-hiddenVisually">Page</span>
            {{@count}}
          </a>
        {{/compare}}
      </li>
    {{/iterate}}
  </ul>
</nav>
