import { Story, Preview, Meta } from '@storybook/addon-docs/blocks';
import { withKnobs, text, boolean, select } from '@storybook/addon-knobs';
import messageTemplate from './message.twig';
import elementsDemo from './demo/elements.twig';
import messageWithCloseDemo from './demo/message-with-close.twig';
import warningDemo from './demo/warning.twig';
import '../../themes/_index.scss';
import './message.scss';

<Meta title="Objects/Message" decorators={[withKnobs]} />

# Message

This is where a description will appear.

<Preview>
  <Story name="Elements" height="100px">
    {messageTemplate({
      label: text('first label'),
      includeClose: boolean('include close button')
    })}
  </Story>
</Preview>

## Message with Close Button 

A message with a close button

<Preview>
  <Story name="Message with close" height="100px">
    {messageTemplate({
      label: text('first label'),
      includeClose: true
    })}
  </Story>
</Preview>

## Warning 

A warning message

<Preview>
  <Story name="Warning" height="100px">
    {warningDemo({
      label: text('first label')
    })}
  </Story>
</Preview>