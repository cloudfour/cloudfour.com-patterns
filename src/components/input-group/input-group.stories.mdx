import { Story, Canvas, Meta } from '@storybook/addon-docs/blocks';
import template from './input-group.twig';
import inputGroupDemo from './demo/input-group-demo.twig';
import './input-group.scss';
import './demo/styles.scss';

<Meta title="Components/Input Group" />

## Input Group

The Input Group component combines the Button and Input components. Buttons and inputs can be put in whatever order you choose, and the components will adjust accordingly.

<Canvas>
  <Story name="Example">{(args) => inputGroupDemo(args)}</Story>
</Canvas>
