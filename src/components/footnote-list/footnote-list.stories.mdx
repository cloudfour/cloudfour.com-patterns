import { Story, Canvas, Meta } from '@storybook/addon-docs/blocks';
import template from './footnote-list.twig';

<Meta
  title="Components/Footnote/Footnote List"
  argTypes={{
    count: { type: { name: 'number' } },
    content: { type: { name: 'string' } },
  }}
/>

# Footnote List

Used for displaying one or more Footnote List Items (TODO: link).

## Basic usage

TODO: code example
<Canvas>
  <Story name="Basic" args={{
    count: 1,
  }}>
    {(args) => template({
      items: [
        'Footnote 1',
        'Footnote 2',
        'Footnote 3',
        'Footnote 4',
      ]
    })}
  </Story>
</Canvas>

## Template Properties

- `items`: An array of strings

## Template Blocks

- `content`: The footnote contents (overwrites anything placed in the items property)
