import { Canvas, Controls, Meta } from '@storybook/addon-docs/blocks';

import * as LightYoutubeStories from './light-youtube.stories.js';

<Meta of={LightYoutubeStories} />

# lite-youtube

Our patterns include a `lite-youtube-fallback` class for use with [the `lite-youtube` web component](https://github.com/justinribeiro/lite-youtube).

<Canvas of={LightYoutubeStories.Default} />

<Controls of={LightYoutubeStories.Default} />
