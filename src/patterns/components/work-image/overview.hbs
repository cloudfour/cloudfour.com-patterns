---
notes: |
  Showcase work examples inside of browser- and device-themed frames (with
  whimsical tilt).

  The `<defs>` block should only be included once per page (additional
  occurrences will reference that first set of definitions).
---

{{{
  embed "patterns.components.work-image.base"
  includeDefs=true
  large="/images/team/team-photo.jpg"
  small="/images/team/sara.png"
}}}
