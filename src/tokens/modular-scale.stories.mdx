import { Meta } from '@storybook/addon-docs/blocks';
import tokens from '../compiled/tokens/js/tokens';
import _ from 'lodash'
const modularScale = tokens.number.scale.modular;
const modularScaleRows = Object.keys(modularScale).map((key) => {
  const { name, attributes, value } = modularScale[key];
  return (
    <tr key={name}>
      <td>
        <code>
          scale.${_.kebabCase(`${attributes.item}-${attributes.subitem}`)}
        </code>
      </td>
      <td>
        <code>{value}</code>
      </td>
    </tr>
  );
});

<Meta title="Design/Tokens/Modular Scale" />

# Modular Scale

```scss
@use "../../compiled/tokens/scss/scale";
$example: scale.$modular-ratio; // => 1.25
```

```javascript
import tokens from '../../compiled/tokens/js/tokens';
console.log(tokens.number.scale.modular.ratio.value); // => 1.25
```

## Modular Scale

<table>
  <thead>
    <tr>
      <th style={{ width: '10%' }}>Name</th>
      <th>Value</th>
    </tr>
  </thead>
  <tbody>{modularScaleRows}</tbody>
</table>
