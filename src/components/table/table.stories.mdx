import { Story, Preview, Meta } from '@storybook/addon-docs/blocks';
import { withKnobs, boolean } from '@storybook/addon-knobs';
import basicDemo from './demo/basic.twig';
import data from './demo/data.json';
import './table.scss';

<Meta title="Components/Table" decorators={[withKnobs]} />

# Table Object

The `c-table` class applies default styling to HTML `table` elements.
A table can optionally headers and footers.

<Preview>
  <Story name="Basic">
    {basicDemo({
      hasHeader: boolean('Include Header', true),
      hasFooter: boolean('Include Footer', true),
      isRuled: boolean('Add c-tabled--ruled modifier to the table', false),
      isStriped: boolean('Add c-tabled--striped modifier to the table', false),
      numericCells: boolean(
        'Add c-table__numeric-cell modifier to cells',
        false
      ),
      rightAlignRowHeadings: boolean('Right-align row Headings:', false),
      tableData: data
    })}
  </Story>
</Preview>

# Ruled Table

The `c-table--ruled` modifier adds horizontal rules in between table rows.

<Preview>
  <Story name="Ruled">
    {basicDemo({
      hasHeader: boolean('Include Header', true),
      hasFooter: boolean('Include Footer', true),
      isRuled: true,
      isStriped: boolean('Add c-tabled--striped modifier to the table', false),
      numericCells: boolean(
        'Add c-table__numeric-cell modifier to cells',
        false
      ),
      rightAlignRowHeadings: boolean('Right-align row Headings:', false),
      tableData: data
    })}
  </Story>
</Preview>

# Striped Table

The `c-table--striped` modifier adds alternating background colors to table rows.

<Preview>
  <Story name="Striped">
    {basicDemo({
      hasHeader: boolean('Include Header', true),
      hasFooter: boolean('Include Footer', true),
      isRuled: boolean('Add c-tabled--ruled modifier to the table', false),
      isStriped: true,
      numericCells: boolean(
        'Add c-table__numeric-cell modifier to cells',
        false
      ),
      rightAlignRowHeadings: boolean('Right-align row Headings:', false),
      tableData: data
    })}
  </Story>
</Preview>

# Numeric Data

When cells contain numeric data, aligning them to the right and using tabular
figures can make it easier to compare cells across a column. This can be
achieved by adding c-table\_\_cell--numeric to table cells.

<Preview>
  <Story name="Numeric Data">
    {basicDemo({
      hasHeader: boolean('Include Header', true),
      hasFooter: boolean('Include Footer', true),
      isRuled: boolean('Add c-tabled--ruled modifier to the table', false),
      isStriped: boolean('Add c-tabled--striped modifier to the table', false),
      numericCells: true,
      rightAlignRowHeadings: boolean('Right-align row Headings:', false),
      tableData: data
    })}
  </Story>
</Preview>

# Tighten Row Headings

In complex tables, row headings sometimes begin to feel disconnected from the
data in their row. You can tighten up the visual connection by right-aligning
row headings. To do so, add the `c-table--tighten-row-headings` to the table.

<Preview>
  <Story name="Tighten Row Headings">
    {basicDemo({
      hasHeader: boolean('Include Header', true),
      hasFooter: boolean('Include Footer', true),
      isRuled: boolean('Add c-tabled--ruled modifier to the table', false),
      isStriped: boolean('Add c-tabled--striped modifier to the table', false),
      numericCells: boolean(
        'Add c-table__numeric-cell modifier to cells',
        false
      ),
      rightAlignRowHeadings: true,
      tableData: data
    })}
  </Story>
</Preview>
