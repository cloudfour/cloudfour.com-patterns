{#
  These inline styles are necessary to give the theme contents adequate padding
  within the Storybook canvas. Without this wrapper, the theme contents would
  run up against the theme container.

  This would probably not be necessary if an HTML decorator were added to the
  Themes addon.

  @see https://github.com/tonai/storybook-addon-themes#usage-with-decorator
#}
<div class="u-pad-1{% if theme %} t-{{theme}}{% endif %}">
  {% block content %}
    <h1>Hello world</h1>
    <p>Progressive web apps <a href="#">simply make sense</a>!</p>
  {% endblock %}
</div>
