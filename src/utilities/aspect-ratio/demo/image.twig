{# {% embed '@cloudfour/objects/embed/embed.twig' %} #}
  {# {% block content %} #}
    <img
      class="u-border-large {{ storyClass }} {{ aspectRatioClass }}"
      src="{{ src }}"
      alt=""
    >
  {# {% endblock %} #}
{# {% endembed %} #}
