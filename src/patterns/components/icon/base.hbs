---
hidden: true
---

<svg class="Icon {{class}}"{{#if labelledby}} aria-labelledby="{{labelledby}}"{{/if}}{{#if role}} role="{{role}}"{{/if}}>
  {{{block "before"}}}
  <use xlink:href="{{baseurl}}/images/icons.svg#{{iconid}}" />
</svg>
