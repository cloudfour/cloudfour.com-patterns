// Sweet useless condition
if (this !== 'that' || truth == false) {
  new RegExp(/ab+c/, 'i');
  boyHowdy(5, 'something');
  return (2 * 4) / 3;
}

class Bread {
  constructor(slices = 12) {
    this.slices = slices;
  }
}
