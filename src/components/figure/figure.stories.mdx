import { Story, Preview, Meta } from '@storybook/addon-docs/blocks';
import { withKnobs, boolean } from '@storybook/addon-knobs';
import basicDemo from './demo/basic.twig';
import './figure.scss';

<Meta title="Components/Figure" decorators={[withKnobs]} />

# Figure

The `c-figure` class applies styling to the figure element. Figures may be accompanied by an optional caption.

<Preview>
  <Story name="Basic">
    {basicDemo({
      src: 'https://picsum.photos/600/300',
      alt: 'This is an image',
      hasCaption: boolean('Include Caption', true)
    })}
  </Story>
</Preview>
