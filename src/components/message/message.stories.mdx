import { Story, Canvas, Meta } from '@storybook/addon-docs/blocks';
import messageDemo from './demo/default-message.twig';
import messageWithCloseDemo from './demo/message-with-close.twig';
import './message.scss';

<Meta title="Components/Message" />

# Message

A containing element intended for short, actionable user messages. Originally
designed to notify the user of offline network connection status.

<Canvas>
  <Story
    name="Default"
    argTypes={{
      label: { type: { name: 'string' } },
      include_close: { type: { name: 'boolean' }, defaultValue: false },
    }}
  >
    {(args) => messageDemo(args)}
  </Story>
</Canvas>

## Message with close button

Most messages should allow users to dismiss them with a close button.

<Canvas>
  <Story
    name="With Close Button"
    argTypes={{ label: { type: { name: 'string' } } }}
  >
    {(args) => messageWithCloseDemo(args)}
  </Story>
</Canvas>
