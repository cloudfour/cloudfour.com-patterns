import { Story, Canvas, Meta } from '@storybook/addon-docs/blocks';
import template from './demo/demo.twig';

<Meta
  title="Components/Footnote/Footnote List Item"
  argTypes={{
    count: { type: { name: 'number' } },
    content: { type: { name: 'string' } },
  }}
/>

# Footnote List Item

Used for displaying the content of a footnote. Expected to be wrapped in a Footnote List (TODO: link)

## Basic usage

TODO: code example
<Canvas>
  <Story name="Basic" args={{
    count: 1,
  }}>
    {(args) => template(args)}
  </Story>
</Canvas>

## Template Properties

- `count` (number)

## Template Blocks

- `content` The footnote contents
