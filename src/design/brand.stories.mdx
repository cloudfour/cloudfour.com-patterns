import { Meta, IconGallery, IconItem } from '@storybook/addon-docs/blocks';
import { extname, relative } from 'path';
// Import whole directory of icons together
// @see https://webpack.js.org/guides/dependency-management/#context-module-api
const logoDir = require.context('../assets/brand', true, /\.svg$/);
const logoElements = logoDir.keys().map((key) => {
  const Logo = logoDir(key).default;
  // Retains subdirectory if we want to organize with those in the future
  const name = relative('.', key).replace(extname(key), '');
  return (
    <IconItem name={name}>
      <Logo class="c-icon" />
    </IconItem>
  );
});

<Meta title="Design/Brand" />

# Logo

Cloud Four logo in SVG format. The logo color can be modified via the CSS
`color` property.

<IconGallery>{logoElements}</IconGallery>
