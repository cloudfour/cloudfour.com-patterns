import { Story, Preview, Meta } from '@storybook/addon-docs/blocks';
import { withKnobs, boolean } from '@storybook/addon-knobs';
import basicDemo from './demo/basic.twig';
import './demo/styles.scss';
import './container.scss';

<Meta title="Objects/Container" decorators={[withKnobs]} />

# Containers

Containers are helpful for layouts, they provide a maximum width and center content on large screens.

<Preview>
  <Story
    name="Basic"
    height="300px"
    parameters={{ paddings: { disabled: true } }}
  >
    {basicDemo({
      prose: boolean('Prose', false),
    })}
  </Story>
</Preview>

## Prose

The `--prose` modifier is intended to be used with content that mostly consists of text, like an article. It helps improve readability by limiting line length.

<Preview>
  <Story
    name="Prose"
    height="300px"
    parameters={{ paddings: { disabled: true } }}
  >
    {basicDemo({
      prose: boolean('Prose', true),
    })}
  </Story>
</Preview>

## Themes

<Preview>
  <Story
    name="Themes (Dark)"
    height="300px"
    parameters={{ themes: { disable: true }, paddings: { disabled: true } }}
  >
    {basicDemo({
      prose: boolean('Add o-container--prose modifier to the container', false),
      theme: 'dark',
    })}
  </Story>
</Preview>
