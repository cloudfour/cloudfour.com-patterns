import { Story, Preview, Meta } from '@storybook/addon-docs/blocks';
import { withKnobs, boolean } from '@storybook/addon-knobs';
import template from './calendar-date.twig';

<Meta title="Components/Calendar Date" decorators={[withKnobs]} />

# Calendar Date

<Preview>
  <Story name="Single Day" height="400px">
    {template()}
  </Story>
  <Story name="Multiple Days" height="400px">
    {template({ days: 3 })}
  </Story>
  <Story name="Sold Out" height="400px">
    {template({ sold_out: true })}
  </Story>
</Preview>
