import { Meta } from '@storybook/addon-docs/blocks';
import tokens from '../compiled/tokens/js/tokens';
import _ from 'lodash'
const breakpoints = tokens.size.breakpoint;
const breakpointRows = Object.keys(breakpoints).map((key) => {
  const { name, attributes, value } = breakpoints[key];
  return (
    <tr key={name}>
      <td>
        <code>breakpoint.${_.kebabCase(attributes.item)}</code>
      </td>
      <td>
        <code>{value}</code>
      </td>
    </tr>
  );
});

<Meta title="Design/Tokens/Breakpoint" />

# Breakpoint

```scss
@use "../../compiled/tokens/scss/breakpoint";
$example: breakpoint.$xs; // => "20em"
```

```javascript
import tokens from '../../compiled/tokens/js/tokens';
console.log(tokens.size.breakpoint.xs.value); // => "20em"
```

## Media Query

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Value</th>
    </tr>
  </thead>
  <tbody>{breakpointRows}</tbody>
</table>
